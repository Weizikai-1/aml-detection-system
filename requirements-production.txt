# 生产环境依赖（基于 requirements.txt，新增以下生产级组件）

# ===== API 框架 =====
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # 文件上传支持

# ===== 异步任务队列 =====
celery>=5.3.0
redis>=5.0.0

# ===== 数据库 =====
psycopg2-binary>=2.9.9  # PostgreSQL 驱动
sqlalchemy>=2.0.0  # ORM
alembic>=1.13.0  # 数据库迁移工具

# ===== 认证与安全 =====
passlib[bcrypt]>=1.7.4  # 密码哈希
pyjwt>=2.8.0  # JWT token
python-jose[cryptography]>=3.3.0  # JWT + 加密
cryptography>=41.0.0  # API 密钥加密

# ===== 监控 =====
prometheus-client>=0.19.0  # Prometheus 指标导出

# ===== 限流与缓存 =====
slowapi>=0.1.9  # API 限流

# ===== LLM 并发支持 =====
nest-asyncio>=1.5.0  # 事件循环补丁，支持 LLM 并发调用

# ===== 测试（生产环境压力测试） =====
# locust>=2.0.0  # 压力测试（可选，需要单独安装）
httpx>=0.26.0  # 异步 HTTP 客户端

# ===== 部署工具 =====
gunicorn>=21.0.0  # WSGI 服务器（Uvicorn workers）