# AML 反洗钱检测系统 — 核心依赖
# 基础（规则引擎 + 评估）
numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# LangGraph 多Agent工作流
langgraph>=0.2.0

# DeepSeek LLM（可选，需要 API Key）
openai>=1.0

# GNN 图神经网络（可选，需要 PyTorch 环境）
# torch>=2.0
# torch-geometric>=2.4

# FastAPI 接口
fastapi>=0.100
uvicorn>=0.20

# Streamlit 界面
streamlit>=1.28

# 测试
pytest>=8.0
